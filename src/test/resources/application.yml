

spring:
  profiles:
    test
#  datasource:
##    url: jdbc:postgresql://localhost:5432/turistmo
##    username: fazli
#    hikari:
#      jdbcUrl:  jdbc:postgresql://localhost:5432/turistmo
#      username: fazli
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update
    generate-ddl: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  h2:
    console:
      enabled: true

---
